# API base URL (no trailing slash).
# Leave empty in local dev so Vite proxies /v1 → http://localhost:8000
# For production / preview against a remote API:
# VITE_API_BASE_URL=https://your-api.example.com

VITE_API_BASE_URL=
